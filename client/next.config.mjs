/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Bu yerda boshqa sozlamalar bo'lishi mumkin, lekin `src` papkasidan foydalanish uchun qo'shimcha o'zgartirishlar kiritish shart emas.
  }
  
  export default nextConfig;
  